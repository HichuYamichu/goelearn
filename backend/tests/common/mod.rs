// Dont use mock connection in order to test actual behavior

pub fn make_schema() {
    // let schema = Schema::build(
    //     Query::default(),
    //     Mutation::default(),
    //     Subscription::default(),
    // )
    // .data(redis_client)
    // .data(s3_bucket.clone())
    // .extension(Tracing)
    // .finish();
}
